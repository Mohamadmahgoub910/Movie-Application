@{
    ViewData["Title"] = "تأكيد إرسال رابط إعادة التعيين";
}

<style>
    .confirmation-container {
        max-width: 600px;
        margin: 50px auto;
        text-align: center;
    }

    .confirmation-card {
        background: white;
        border-radius: 15px;
        box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        padding: 3rem;
    }

    .success-icon {
        font-size: 5rem;
        color: #28a745;
        margin-bottom: 1rem;
    }
</style>

<div class="confirmation-container">
    <div class="confirmation-card">
        <i class="fas fa-envelope-open-text success-icon"></i>
        <h2 class="mb-4">تم إرسال رابط إعادة التعيين!</h2>
        <p class="lead">تحقق من بريدك الإلكتروني</p>
        <p>لقد أرسلنا رابط إعادة تعيين كلمة المرور إلى بريدك الإلكتروني.</p>
        <div class="alert alert-info mt-4">
            <i class="fas fa-info-circle me-2"></i>
            الرابط صالح لمدة ساعة واحدة فقط
        </div>
        <a asp-action="Login" class="btn btn-primary mt-3">
            <i class="fas fa-sign-in-alt me-2"></i>العودة لتسجيل الدخول
        </a>
    </div>
</div>